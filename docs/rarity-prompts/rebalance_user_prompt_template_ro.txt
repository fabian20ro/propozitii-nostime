Returnează DOAR JSON valid: array de numere întregi (`local_id`).
Fără text extra, fără markdown, fără blocuri de cod.

Task:
- Alege cele mai comune intrări din lista de mai jos.
- Răspunsul trebuie să conțină DOAR `local_id`.
- Numărul exact de intrări este impus de schema JSON.

Reguli hard:
- Fără duplicate.
- Nu inventa id-uri.
- Folosește numai `local_id`-urile din input.
- Dacă nu ești sigur, fă cea mai bună alegere.
- Evită termeni vulgari/obsceni când există alternative.

Input:
{{INPUT_JSON}}
