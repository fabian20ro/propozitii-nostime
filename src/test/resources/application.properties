# Use lightweight Alpine image for Dev Services PostgreSQL container
quarkus.datasource.devservices.image-name=postgres:16-alpine

# Run schema + test seed migrations on startup
quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=classpath:db/migration,classpath:db/testmigration
